const config = {
  population: {
    table: 'analytics.census_data',
    connection: {
      host: '{{{host}}}',
      user: '{{{user}}}',
      password: '{{{password}}}',
    },
  },
};

module.exports = config;
